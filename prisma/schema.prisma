generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Bike {
  id String @id @default(cuid())
  make String
  model String
  year Int
  type String
}
